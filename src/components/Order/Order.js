import React from 'react';
import Footer from '../Footer/Footer';
import MainNavbar from '../MainNavbar/MainNavbar';

const Order = () => {
    return (
        <div>
            <MainNavbar></MainNavbar>

            <div>
                <h1>This is order poage</h1>
            </div>

            <Footer></Footer>
        </div>
    );
};

export default Order;